import asyncio
class ProfitAdapter:
    async def generate(self, profile, context):
        await asyncio.sleep(0.2)
        return {"type":"finance","model":"profit","insight":"Projeção ajustada com base no perfil."}
